{
  "name": "ttg",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "./deploy.sh",
    "create-local": "graph create --node http://localhost:8020/ mzero-labs/ttg",
    "remove-local": "graph remove --node http://localhost:8020/ mzero-labs/ttg",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mzero-labs/ttg",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
