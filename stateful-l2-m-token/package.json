{
  "name": "stateful-l2-m-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy m-token-hyperliquid/0.2.0 --path .",
    "create-local": "graph create --node http://localhost:8020/ mzero/m-token",
    "remove-local": "graph remove --node http://localhost:8020/ mzero/m-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ mzero/m-token",
    "test": "graph test",
    "prettier": "prettier --write 'src/**/*.ts' 'scripts/**/*.js'"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0",
    "dotenv": "^16.4.5",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=23.2.0"
  }
}
