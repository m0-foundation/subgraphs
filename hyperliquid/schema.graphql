type MToken_IndexUpdated {
  id: ID!
  index: BigInt!
  timestamp: Int!
  blockNumber: Int!
}

type MToken_StartedEarning {
  id: ID!
  account: String!
  timestamp: Int!
  blockNumber: Int!
}

type MToken_StoppedEarning {
  id: ID!
  account: String!
  timestamp: Int!
  blockNumber: Int!
}

type MToken_Transfer {
  id: ID!
  sender: String!
  recipient: String!
  amount: BigInt!
  timestamp: Int!
  blockNumber: Int!
}

type USDHL_YieldClaimed {
  id: ID!
  amount: BigInt!
  timestamp: Int!
  blockNumber: Int!
}
